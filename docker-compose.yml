
services:
  flask-app-NotePaper:
    # 指定容器名称
    container_name: NotePaper
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: production
      FLASK_APP: app.py


